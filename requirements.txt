# =============================================================================
# AdLens — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# Python: 3.10+ (tested on 3.14)
# System: FFmpeg must be on PATH (see README.md for installation)
# =============================================================================

# --- Web framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# --- Data models & config ---
pydantic>=2.7.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# --- Video download ---
yt-dlp>=2024.8.6

# --- Computer vision / frame sampling ---
# Use headless to avoid GUI dependencies on server/CI
opencv-python-headless>=4.10.0

# --- Scene detection ---
scenedetect>=0.6.4

# --- OCR ---
easyocr>=1.7.1
Pillow>=10.4.0

# --- Local ASR (optional — downloaded on first use, ~150 MB) ---
# Comment out if you only want to use the OpenAI Whisper API
faster-whisper>=1.1.0

# --- OpenAI SDK (for Whisper API + GPT-4o-mini vision) ---
openai>=1.50.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0  # for FastAPI TestClient

# --- Utilities ---
requests>=2.32.0
